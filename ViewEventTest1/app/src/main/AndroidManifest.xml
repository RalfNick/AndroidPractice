<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.ralf.vieweventtest1">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".EventTransmitActivity">
        </activity>
        <activity android:name=".ViewGroupAndVGActivity">
        </activity>
        <activity android:name=".ViewGroupAndViewActivity">
        </activity>
        <activity android:name=".SingleViewGroupActivity">
        </activity>
        <activity android:name=".SingleViewActivity">
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>